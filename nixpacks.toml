[phases.setup]
nixPkgs = ["python313", "postgresql"]

[phases.install]
cmds = ["python -m venv /app/.venv", ". /app/.venv/bin/activate && pip install -r requirements.txt"]

[start]
cmd = ". /app/.venv/bin/activate && gunicorn bot:app"
